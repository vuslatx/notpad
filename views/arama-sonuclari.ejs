<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Arama Sonuçları</title>
</head>
<body>
    <h1>Arama Sonuçları: "<%= searchQuery %>"</h1>
    <% if (notes.length > 0) { %>
        <% notes.forEach(function(note) { %>
            <div>
                <h2><a href="/notes/<%= note.id %>"><%= note.title %></a></h2>
                <p><%= note.content.substring(0, 100) %>...</p>
            </div>
        <% }); %>
    <% } else { %>
        <p>Aradığınız kriterlere uygun not bulunamadı.</p>
    <% } %>
    <a href="/">Ana Sayfaya Dön</a>
</body>
</html>
